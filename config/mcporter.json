{
  "mcpServers": {
    "zapier": {
      "description": "Zapier MCP — Twitter/X, Gmail, and 8000+ app integrations",
      "baseUrl": "https://mcp.zapier.com/api/v1/connect",
      "headers": {
        "Authorization": "Bearer ${ZAPIER_MCP_TOKEN}"
      }
    },
    "heygen": {
      "description": "HeyGen MCP — AI video generation via Remote OAuth (uses HeyGen plan credits)",
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://mcp.heygen.com/mcp/v1/"]
    },
    "gmail": {
      "description": "Gmail MCP — read, send, search, draft, label, and manage emails via Google Gmail API",
      "command": "npx",
      "args": ["@dev-hitesh-gupta/gmail-mcp-server"]
    },
    "chrome-devtools": {
      "description": "Chrome DevTools MCP — browser automation, debugging, screenshots, Lighthouse audits, network inspection",
      "command": "npx",
      "args": ["chrome-devtools-mcp@latest", "--no-performance-crux", "--no-usage-statistics"]
    }
  }
}
